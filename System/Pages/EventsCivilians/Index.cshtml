@page
@model RegisterSystem.Pages.Party.EventsCiviliansPage
@using RegisterSystem.Pages.Extensions
@{  ViewData["Title"] = "Index"; }

<h1>Index</h1>

<p> <a asp-page="Create" asp-page-handler="Create">Create New</a> </p>
<table class="table">
    <thead>
        <tr>
            <th> @Html.DisplayNameFor(model => model.Items[0].EventId) </th>
            <th> @Html.DisplayNameFor(model => model.Items[0].CivilianId) </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Items) {
        <tr>
            <td> @Html.DisplayFor(modelItem => item.EventId) </td>
            <td> @Html.DisplayFor(modelItem => item.CivilianId) </td>
            <td>
                <a asp-page="./Edit" asp-page-handler="Edit" asp-route-id="@item.Id">Edit</a>&nbsp;
                <a asp-page="./Details" asp-page-handler="Details" asp-route-id="@item.Id">Details</a>&nbsp;
                <a asp-page="./Delete" asp-page-handler="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>